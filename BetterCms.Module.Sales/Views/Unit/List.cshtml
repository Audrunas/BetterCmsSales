@using BetterCms.Module.Root;
@using BetterCms.Module.Root.Mvc.Grids;
@using BetterCms.Module.Root.ViewModels.Shared;
@using BetterCms.Module.Sales.Content.Resources

@{
    var gridViewModel = new EditableGridViewModel
    {
        ShowMessages = true,
        TopBlockTitle = SalesGlobalization.SiteSettings_Units_Title,
        Columns = new List<EditableGridColumn>
            {
                new EditableGridColumn(SalesGlobalization.SiteSettings_Units_Title_Title, "Title", "title")
                    {
                        AutoFocus = true
                    },
                new EditableGridColumn(SalesGlobalization.SiteSettings_Units_ShortTitle_Title, "ShortTitle", "shortTitle")
            }
    };
}

@Html.Partial(RootModuleConstants.EditableGridTemplate, gridViewModel)